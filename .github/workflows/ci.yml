name: vAG-Apps CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      # Repo has zero runtime deps and no package-lock.json — npm ci would
      # fail on the missing lockfile. npm install is equivalent here.
      - run: npm install --ignore-scripts
      - run: npm run validate
      - run: npm run build:index
      - run: git diff --exit-code apps/index.json || true
